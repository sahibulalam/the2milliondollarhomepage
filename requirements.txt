# Nothing is required to run this. The standard library covers the server and
# SQLite, which is what a host with a disk (Railway, Render, Fly) uses.
#
# Uncomment only for a serverless host with no filesystem, where DATABASE_URL
# points at Postgres instead:
# psycopg2-binary==2.9.9
